let numero;

function contar(numero){
    num = numero;
    let contador;
    let consola = "";
    for(contador = 1;contador <= num;contador++){
        consola += contador+"\n";
    }
    return consola
}

const expcontar = function(numero){
    num = numero;
    let contador;
    let consola = "";
    for(contador = 1;contador <= num;contador++){
        consola += contador+"\n";
    }
    return consola
}