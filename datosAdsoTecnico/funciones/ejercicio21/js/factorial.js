let numero;

function factor(numero){
    let contador;
    let factorial =1;
    num = numero;
    for(contador=1;contador <= num;contador++){
        factorial = factorial * contador;
    }    
    return factorial
}

const expfactor = function(numero){
    let contador;
    let factorial =1;
    num = numero;
    for(contador=1;contador <= num;contador++){
        factorial = factorial * contador;
    }    
    return factorial
}
