let numero;

function factor(numero){
    let contador = 0;
    let factorial = 1;
    num = numero;
    while(contador < num){
        contador++;
        factorial = factorial * contador;
    }
    return factorial;
}

const expfactor = function(numero){
    let contador = 0;
    let factorial = 1;
    num = numero;
    while(contador < num){
        contador++;
        factorial = factorial * contador;
    }
    return factorial;
}
